services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: roovea
      POSTGRES_PASSWORD: postgres
      POSTGRES_USER: postgres
    ports:
      - "5432:5432"
    volumes:
      - roovea-postgres:/var/lib/postgresql/data

volumes:
  roovea-postgres:
